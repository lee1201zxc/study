map과 비슷하지만 정렬되어 있지 않음.

삽입, 삭제, 탐색 모두 O(1)만에 됨.

헤더파일 추가 : include<unordered_map);



unordered_map<int,int> v; -> 생성

v[i] -> i번째 원소 참조
v.clear() -> 모든 원소 삭제
v.insert(make_pair(key,value)) -> key,value 값 추가
v.find(key) -> key값을 가진 원소 찾음.
v.count(key) -> 특정 key값을 가지는 원소의 개수
v.size()-> 원소의 갯수 리턴 
v.empty() -> 비어있는지 확인
v.swap(a,b) -> a와b의 원소와 capacity 바꿈
v.erase(key) -> key의 원소삭제
