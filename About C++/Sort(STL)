정렬을 도와줌

algorithm 선언후 사용 #include<algorithm>

사용법

sort(a,a+10) ---->>   a->배열의 시작점 주소, a+10->마지막 주소 +1 
                      이경우 배열a에서 앞에있는 10개의 원소가 오름 차순을 정렬됨.

/*
bool compare(int a, int b)
{
  return a<b;
}
sort(a,a+10,compare);
*/
이 코드는 7번 코드와 같음 compare는 숨겨져 있던거임,compare 를 바꿔서 정렬 방법을 바꿀수 있음.
