ios::sync_with_stdio(false);
cin.tie(0);
cout.tie(0);
    -> 쓰면 입출력속도 printf,scanf와 거의 비슷해짐 (메모리는 아님)
    
#include <bits/stdc++.h> -> 많이 쓰이는 헤더들 모아둠 (대회에서 유용)

cout, cin은 printf, scanf에 비해 느리지만 편하게 쓸 수 있다.

cout<<endl; 보다 cout<<"\n"; 이 더 빠르다.

배열 초기화시 
fill_n이나 fill이용(2차원배열시)
fill_n(a,5,1) -> a에서 시작해서 5개를 1로 초기화 한다.
fill(a[0],a[0]+25,1) -> a[0]에서 시작해서 a[0]+25까지 1로 초기화 한다. (2차원 배열에서 사용)
