ios::sync_with_stdio(false);
cin.tie(0);
cout.tie(0);
    -> 쓰면 입출력속도 printf,scanf와 거의 비슷해짐 (메모리는 아님)
    
#include <bits/stdc++.h> -> 많이 쓰이는 헤더들 모아둠 (대회에서 유용)

cout, cin은 printf, scanf에 비해 느리지만 편하게 쓸 수 있다.

cout<<endl; 보다 cout<<"\n"; 이 더 빠르다.
